{
  "skill_name": "ingest",
  "evals": [
    {
      "id": 1,
      "name": "single-new-source",
      "prompt": "There's a new source file at docs/sources/redis-eviction-fix.md (jira:INFRA-142) that hasn't been ingested yet — it's a ticket about the Redis cache OOM-crashing because it runs with the noeviction policy, fixed by switching to allkeys-lru. Ingest it into the wiki.",
      "expected_output": "One new wiki page (or an update to the existing Redis Cache page) citing jira:INFRA-142, correct frontmatter, at least 2 outbound wikilinks, source-manifest/catalog/log updated, a lint-clean local git commit staged via propose-pr. Basic single-source sequence correctness.",
      "files": [
        "fixture: run build_fixture.py <dest> from this directory, then point the agent at <dest>",
        "grader: run grade.py <iteration_dir> from this directory and point the agent at <iteration_dir>"
      ],
      "expectations": [
        {"key": "manifest-entry", "text": "docs/source-manifest.jsonl contains an entry for jira:INFRA-142"},
        {"key": "page-cites-source", "text": "A wiki page's `sources:` frontmatter lists jira:INFRA-142"},
        {"key": "wikilink-count", "text": "That page has at least 2 outbound [[wikilinks]]"},
        {"key": "catalog-updated", "text": "docs/catalog.jsonl reflects the new/updated page"},
        {"key": "log-entry", "text": "docs/log.jsonl has an `ingest` action entry mentioning INFRA-142"},
        {"key": "single-commit", "text": "Exactly one git commit was created for the session"},
        {"key": "lint-clean", "text": "`wiki-toolkit lint` reports no violations on the final state"},
        {"key": "unrelated-untouched", "text": "The three unrelated already-covered sources (INFRA-100, AUTH-200, infra-overview-v1) were left untouched"},
        {"key": "inline-citation", "text": "If the page synthesizes 3+ sources, at least one claim sourced from jira:INFRA-142 is cited inline with `^[jira:INFRA-142]`; below that threshold (as in this fixture) the check is automatically satisfied"}
      ]
    },
    {
      "id": 2,
      "name": "multi-source-one-session",
      "prompt": "Three new sources about the rate-limiting middleware just landed under docs/sources/: ratelimit-design-doc.md (confluence:RATE-001, the design doc), ratelimit-implementation-ticket.md (jira:RATE-002, the implementation ticket), and ratelimit-slack-thread.md (slack:rate-limit-launch, the launch announcement). Ingest all three in one session.",
      "expected_output": "One coherent ingest session covering all 3 sources — one source-scan, one build, one cross-linker pass, one lint, one propose-pr for the whole session (not one PR per source), each page citing the right source(s), at least 2 outbound wikilinks per page (stubs created if needed), log entries for each source. Tests the one-session/one-PR batching rule and multi-source citation.",
      "files": [
        "fixture: run build_fixture.py <dest> from this directory, then point the agent at <dest>",
        "grader: run grade.py <iteration_dir> from this directory and point the agent at <iteration_dir>"
      ],
      "expectations": [
        {"key": "all-sources-in-manifest", "text": "confluence:RATE-001, jira:RATE-002, and slack:rate-limit-launch all appear in the final source-manifest.jsonl"},
        {"key": "all-sources-cited", "text": "Every one of the three sources is cited by at least one wiki page's `sources:` frontmatter (none dropped)"},
        {"key": "single-commit", "text": "Exactly one git commit/branch was created for the whole session, not one per source"},
        {"key": "wikilink-count", "text": "Every new or touched page has at least 2 outbound [[wikilinks]]"},
        {"key": "log-entry-per-source", "text": "docs/log.jsonl has one `ingest` action entry per source (3 entries total for this session)"},
        {"key": "lint-clean", "text": "`wiki-toolkit lint` reports no violations on the final state"},
        {"key": "relationships-entry", "text": "At least one page has a `relationships:` entry enriching an existing [[wikilink]] in its body"}
      ]
    },
    {
      "id": 3,
      "name": "update-to-covered-source",
      "prompt": "docs/sources/jira-auth-200.md (jira:AUTH-200) was just updated — the JWT token expiry changed from 24h to 1h. This source is already covered by the existing Auth Service wiki page. Make sure the wiki reflects the change.",
      "expected_output": "Agent recognizes this as an update to an already-covered source (source-scan flags it 'needs --accept-covered'), confirms the change is real, re-scans with --accept-covered, and updates the Auth Service page's content (24h -> 1h) rather than creating a duplicate page or silently accepting the other two unrelated already-covered sources (INFRA-100, infra-overview-v1) that also show up as needing accept-covered but weren't actually asked about.",
      "files": [
        "fixture: run build_fixture.py <dest> from this directory, then point the agent at <dest>",
        "grader: run grade.py <iteration_dir> from this directory and point the agent at <iteration_dir>"
      ],
      "expectations": [
        {"key": "content-reflects-update", "text": "The Auth Service wiki page's body reflects the 1h expiry, not only the stale 24h claim"},
        {"key": "accept-covered-used", "text": "jira:AUTH-200 was accepted via --accept-covered (manifest/log shows it was reprocessed)"},
        {"key": "unrelated-untouched", "text": "The two unrelated flagged sources (INFRA-100, infra-overview-v1) were NOT accepted or modified"},
        {"key": "log-entry", "text": "`docs/log.jsonl` records an update/ingest entry mentioning AUTH-200"},
        {"key": "lint-clean", "text": "`wiki-toolkit lint` reports no violations on the final state"},
        {"key": "single-commit", "text": "Exactly one git commit was created for the session"}
      ]
    }
  ]
}
